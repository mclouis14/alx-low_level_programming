#include "main.h"
/**
 * mul - the function to ascertain for a digit (0 through).
 * @a: a - Variable
 * @b: b - variable
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
